---
description: 技术门户信息架构与导航规范——主导航、页脚、面包屑、路由命名
alwaysApply: false
globs: "**/layout*.{tsx,jsx,vue},**/nav*.{tsx,jsx,vue,ts},**/Header*.{tsx,jsx,vue},**/Sidebar*.{tsx,jsx,vue},**/routes*.{ts,tsx,js},**/menu*.{ts,tsx,js}"
---

# 信息架构与导航

## 主导航（≤ 6 项）

推荐结构：

| 项 | 用途 |
|---|---|
| 产品/能力 | 能力目录与选型 |
| 文档 | 指南 + API 参考入口 |
| 解决方案 / 场景 | 按业务场景聚合（可选） |
| 示例 / 资源 | 示例代码、SDK、工具 |
| 控制台 | 跳转已登录产品（外链可） |
| 支持 | FAQ、工单、状态页 |

禁止：把每个微服务都做成一级导航。

## 路由约定

```
/                     # 门户首页
/works                # 能力目录
/works/[slug]         # 单能力落地页
/docs                 # 文档中心
/docs/[...slug]       # 文档正文
/examples             # 示例列表
/examples/[slug]      # 示例详情
```

- slug 用 kebab-case；路径与导航文案语义对齐
- 外链控制台/旧站：新标签打开，并标明「外部」

## UI 规则

- 当前路由高亮；深页必须有面包屑（首页除外）
- 侧边栏仅用于文档/多级目录，不替代全局主导航
- 移动端：折叠菜单保留与行面相同信息层级，不删关键入口

## 反模式

```tsx
// ❌ 一级导航堆满微服务
const nav = ['用户中心', '订单', '库存', '支付', '消息', '日志', '监控', '配置']

// ✅ 按用户任务聚合
const nav = ['产品', '文档', '示例', '控制台', '支持']
```
