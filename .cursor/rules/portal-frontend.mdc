---
description: 技术门户前端实现规范——布局、组件边界、搜索、主题与设计系统对齐
alwaysApply: false
globs: "**/*.{tsx,jsx,vue,css,scss}"
---

# 门户前端规范

## 页面类型与布局

| 类型 | 布局要点 |
|---|---|
| 首页 | 品牌 + 一句话价值 + 主 CTA（文档/控制台）+ 能力入口；非仪表盘 |
| 能力落地页 | 是什么 → 关键能力 → 快速开始 → 文档/控制台链 |
| 文档页 | 顶栏 + 左侧目录 + 正文 + 可选右侧大纲 |
| 列表面 | 筛选/搜索 + 结果列表；避免无交互装饰卡片墙 |

## 组件边界

- 导航、页脚、文档目录、搜索框做成共享布局组件
- 业务内容（MDX/CMS）与壳层分离，禁止在每个页面复制 Header
- 卡片仅用于可点击的能力/文档入口；纯展示段落不要包卡片

## 搜索

- 文档与产品名必须可被搜索（全文或预建索引）
- 空结果提供热门文档与「联系支持」出口
- 搜索入口在桌面与移动端均常驻可见

## 实现偏好

- 文档路由优先 SSG/静态生成，运行时拉取仅用于个性化
- 代码高亮、目录锚点、复制按钮是文档页标配
- 遵循已有设计系统 token；无设计系统时用 CSS 变量统一色板与间距

## 反模式

```tsx
// ❌ 首页当控制台
<Dashboard stats={...} schedules={...} promos={...} />

// ✅ 首页当入口
<Hero title="数智未来·AI工坊" subtitle="数据能力一站式入口" />
<ProductGrid products={coreProducts} />
```
