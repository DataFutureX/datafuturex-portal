---
description: 技术门户无障碍与性能基线——语义 HTML、键盘、Core Web Vitals、资源策略
alwaysApply: false
globs: "**/*.{tsx,jsx,vue,html,css,scss}"
---

# 无障碍与性能

## 无障碍基线

- 正确标题层级：每页一个 `h1`，不跳级
- 交互控件必须是 `button`/`a`，禁止 `div onClick` 无键盘支持
- 图标按钮有 `aria-label`；装饰图 `alt=""`，信息图有描述性 alt
- 焦点可见；模态框焦点陷阱与 Esc 关闭
- 色对比正文 ≥ 4.5:1；不仅靠颜色传达状态
- 文档目录与主内容可用 Skip link 跳过重复导航

## 性能基线

- 首屏只加载当前路由必要 JS/CSS；文档页按路由拆分
- 图片：明确宽高、现代格式、懒加载非首屏图
- 字体：限制族数与字重；`font-display: swap`
- 第三方脚本（统计、聊天）延迟加载，不挡 LCP
- 目标：LCP < 2.5s，CLS < 0.1，INP < 200ms（常规文档页）

## 检查清单（改 UI 必过）

- [ ] Tab 可走完主流程（导航 → 搜索 → 文档链接）
- [ ] 无横向滚动（375px 宽）
- [ ] 关闭图/关 JS 时核心文案与链接仍可读（文档尤甚）

## 反模式

```tsx
// ❌
<div onClick={goDocs}>文档</div>

// ✅
<Link href="/docs">文档</Link>
```
